# 数据构建代码审查（历史版本）

本文审查的是旧版 HVQ 口径（20 日输入窗口、10 列收益标签），不是当前 AlphaMaster + JKP 版本。当前已改为 8 日窗口、单个 5 日收益标签及原生 Qlib 联合采样；下文具体日期和测试结论不能直接用于新版。

日期：2026-09-16；环境：kbs。审查当前两份 YAML、三个功能模块及 Qlib 实际处理器/采样实现。运行 9 项现有模拟测试及额外隔离实验；读取本地交易日历核对标签终点。没有构建正式数据集，也没有改变现有构建协议。

结论：基础实现及当前日期配置符合既定接口，但不能认定没有数据泄露。明确存在跨划分标签信息重叠；学习历史行还依赖未来标签可用性。现有测试通过不等于完成时间因果性验证。

## 已确认的问题

### 跨划分标签信息重叠

`build.py::prepare_segment` 只按样本日期截取，没有限制最远标签终点。当前最远目标为 t+10：

| 市场 | 划分 | 最后 10 个样本交易日的起始日 | 最后样本的 RET_10D 终点 |
|---|---|---|---|
| CSI300 | train | 2020-12-18 | 2021-01-15 |
| CSI300 | valid | 2022-12-19 | 2023-01-16 |
| CSI300 | test | 2025-12-18 | 2026-01-16 |
| SP500 | train | 2020-12-17 | 2021-01-15 |
| SP500 | valid | 2022-12-16 | 2023-01-17 |
| SP500 | test | 2025-12-17 | 2026-01-15 |

这表示每个划分最后 10 个交易日的 RET_10D 终点超过配置结束日期，不表示这些样本的原始标签一定有效。训练末尾需要验证期行情，验证末尾需要测试期行情，因此不满足严格的标签信息隔离。Qlib 的 handler end_time 限制样本日期，不保证 Ref 负位移不读取 provider 后续日期。

建议：按交易日历和所用最远标签终点剔除 train/valid 跨界样本。测试若定义为“信号日期到 2025 年底”，可以使用 2026 年实现收益；若数据截止也必须是 2025 年，则同样剔除测试末尾样本。

### 学习历史窗口依赖标签可用性

train/valid fetch 使用 DK_L。Qlib PTYPE_A 先执行 infer 处理，再 DropnaLabel 和 CSRankNorm。DropnaLabel 会删除任何标签缺失的历史股票行，随后这些行通过 ffill+bfill 影响窗口输入。

额外模拟实验只把一个历史行的 RET_10D 改成 NaN，保留所有信息特征和共同样本终点不变，输出窗口前 234 列有 171 个单元改变，末日输入及市场窗口保持相同。因此当前接口没有实现“输入窗口独立于标签可用性”。该实验是针对接口依赖关系的反例，未从一条一致的原始 close 序列重新推导全部标签；它不证明现有真实行情中有多少共同有效样本会触发这种变化。实际触发还受十列标签完整性的约束。

建议：输入历史始终取 DK_I 的逐日信息；学习数据只负责目标标签和可训练终点掩码，通过索引对齐。过滤端点而不删除它的历史输入行。该修改会与当前 HVQ 的历史采样协议产生差异，需要明确记录。

### 配置校验有缺口

模拟验证发现以下配置被 load_config 接受：valid start 为 null、市场 handler instruments 与个股 market 不同、个股 end_time 早于测试结束。

当前正式配置没有这些错误，但后续修改配置时可能在读取真实数据后才失败，或者得到错误市场权重。应增加 NaT、加载覆盖范围、两 handler 市场/region/location 一致性检查。当前列数检查也没有验证完整标签名称和表达式顺序，后续同宽配置改动不能仅靠 244 列宽识别。

## 核对正确的部分

- 顺序与切片：feature 0:158、prior 158:171、market 171:234、label 234:244；20 日窗口，当前 float32。
- CSI300 原生 Alpha158；SP500 仅将 VWAP0 表达式替换成 AlphaMaster 典型价格代理，名称和维度保持不变。
- JKP 筛选国家/daily/vw_cap、按名称展开、对齐交易日历、shift(1)、20 日累计收益；模拟未来当日因子变化不影响当日先验。
- 市场算子和顺序沿用参考项目，按独立日历采样；个股历史缺口不改变市场窗口。
- 两 handler 的 RobustZScoreNorm 拟合范围均在 2009–2020；验证/测试信息不参与参数拟合。使用整个训练期拟合适用于训练期结束后的固定模型评估，不等于训练期内逐日滚动回测。
- train/valid 日期为 2009–2020、2021–2022，test 为 2023–2025，样本日期无重叠。验证/测试窗口回看前一划分的历史是正常行为。
- CSRankNorm 只改变同日目标，未直接把目标数值写入 feature/prior/market；test 保留原始目标和 NaN。
- 个股及市场使用截至样本日的 OHLCV；收盘后生成信号适用，不能用于当天开盘或收盘前的决策。窗口内 bfill 不等于使用样本终点之后的信息，但会重复历史行。

## 口径与尚未验证的事项

RET_hD = close(t+h)/close(t+1)-1，并非 close(t+h)/close(t)-1。RET_1D 原始值为零，排名后同日仍相同，没有个股排序信号。train/valid 排名标签与 test 原始收益尺度不同，不能直接跨划分比较收益回归 MSE。

完整 244 列包含未来目标；未来模型只能把前 234 列对应的信息分组作为输入，不能将全部列送入网络。当前阶段没有模型代码，无法审查后续使用是否正确。

未独立核验 JKP 的历史发布版本/修订、指数成分与退市股票覆盖、公司行动复权及行情供应商。这些输入的时点可得性问题不能仅凭 shift(1) 或代码审查排除。真实构建未执行，尚未完成全量缺失、异常及边界标签检查。
